my_list = ['geeksforgeeks', 'geeks', 'geek', 'geezer']

my_list.sort()
length = i = j = 0
n,m = len(my_list[0]), len(my_list[-1])
while (i < n and j < m and my_list[i] == my_list[j]):
    length += 1
    i += 1
    j += 1
print (length-1)